const SearchService = require("./SearchService");

describe("Sample testing", () => {
    it("should check for search funtion", () => {
        // const type = await (typeof SearchService.search("hello"));
        // expect(type).toBe("function");
    });
});